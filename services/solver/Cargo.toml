[package]
name = "solver"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
tokio.workspace = true
serde_json.workspace = true
types = { path = "../../crates/types" }
chain-adapters = { path = "../../crates/chain-adapters" }

[dev-dependencies]
validator = { path = "../validator" }
